---
layout: base.njk
title: 安装教程 - 龙虾历险记
---

<header class="page-header">
  <h1 class="page-title">OpenCRAW 安装入门</h1>
  <p class="page-description">手把手教你搭建开发环境，快速启动爬虫服务</p>
</header>

<article class="content">
  <h2>环境要求</h2>
  <p>在开始之前，请确保你的开发环境满足以下要求：</p>

  <table style="width: 100%; border-collapse: collapse; margin: var(--space-md) 0;">
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><strong>操作系统</strong></td>
      <td style="padding: var(--space-sm);">Linux (Ubuntu 20.04+) / macOS / Windows (WSL2)</td>
    </tr>
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><strong>Python</strong></td>
      <td style="padding: var(--space-sm);">3.10 或更高版本</td>
    </tr>
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><strong>Node.js</strong></td>
      <td style="padding: var(--space-sm);">18.x 或更高版本（用于前端和工具链）</td>
    </tr>
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><strong>Git</strong></td>
      <td style="padding: var(--space-sm);">用于代码版本管理</td>
    </tr>
    <tr>
      <td style="padding: var(--space-sm);"><strong>Docker</strong></td>
      <td style="padding: var(--space-sm);">20.x+（推荐，用于容器化部署）</td>
    </tr>
  </table>

  <h2>快速开始（推荐）</h2>
  <p>使用 Docker 可以最快速地启动 OpenCRAW，无需手动配置环境：</p>

  <h3>1. 克隆项目</h3>
  <pre><code class="language-bash">git clone https://github.com/openclaw/openclaw.git
cd openclaw</code></pre>

  <h3>2. 启动服务</h3>
  <pre><code class="language-bash"># 使用 Docker Compose 一键启动
docker-compose up -d</code></pre>

  <p>首次启动会自动拉取镜像并初始化数据库。服务启动后访问：</p>
  <ul>
    <li>管理后台：<a href="http://localhost:8000">http://localhost:8000</a></li>
    <li>API 文档：<a href="http://localhost:8000/docs">http://localhost:8000/docs</a></li>
  </ul>

  <h2>本地开发环境搭建</h2>
  <p>如果你想进行二次开发，需要搭建本地环境：</p>

  <h3>1. 克隆项目</h3>
  <pre><code class="language-bash">git clone https://github.com/openclaw/openclaw.git
cd openclaw</code></pre>

  <h3>2. 安装 Python 依赖</h3>
  <pre><code class="language-bash"># 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt</code></pre>

  <h3>3. 安装系统依赖</h3>
  <pre><code class="language-bash"># Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y build-essential libpq-dev redis-server

# macOS
brew install postgresql redis

# 启动 Redis
redis-server</code></pre>

  <h3>4. 配置环境变量</h3>
  <pre><code class="language-bash"># 复制环境配置模板
cp .env.example .env

# 编辑配置文件
vim .env</code></pre>

  <p>关键配置项说明：</p>
  <table style="width: 100%; border-collapse: collapse; margin: var(--space-md) 0;">
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><code>DATABASE_URL</code></td>
      <td style="padding: var(--space-sm);">PostgreSQL 连接地址</td>
    </tr>
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><code>REDIS_URL</code></td>
      <td style="padding: var(--space-sm);">Redis 连接地址</td>
    </tr>
    <tr style="border-bottom: 1px solid var(--border-color);">
      <td style="padding: var(--space-sm);"><code>LOG_LEVEL</code></td>
      <td style="padding: var(--space-sm);">日志级别 (DEBUG/INFO/WARNING)</td>
    </tr>
    <tr>
      <td style="padding: var(--space-sm);"><code>CRAWLER_CONCURRENCY</code></td>
      <td style="padding: var(--space-sm);">并发数，默认 100</td>
    </tr>
  </table>

  <h3>5. 初始化数据库</h3>
  <pre><code class="language-bash"># 自动创建数据库和表
python scripts/init_db.py</code></pre>

  <h3>6. 安装 Playwright（可选）</h3>
  <p>如果需要爬取 JavaScript 渲染的页面：</p>
  <pre><code class="language-bash">playwright install chromium
playwright install-deps</code></pre>

  <h3>7. 启动服务</h3>
  <pre><code class="language-bash"># 开发模式（热重载）
python -m openclaw dev

# 或使用 uvicorn
uvicorn openclaw.main:app --reload --port 8000</code></pre>

  <h2>验证安装</h2>
  <p>服务启动后，可以通过以下方式验证：</p>

  <h3>1. 检查 API 健康状态</h3>
  <pre><code class="language-bash">curl http://localhost:8000/health</code></pre>
  <p>预期返回：<code>{"status": "ok", "version": "1.0.0"}</code></p>

  <h3>2. 访问 API 文档</h3>
  <p>打开浏览器访问 <a href="http://localhost:8000/docs">http://localhost:8000/docs</a></p>
  <p>可以看到所有 API 接口的交互式文档。</p>

  <h3>3. 创建第一个任务</h3>
  <pre><code class="language-bash">curl -X POST http://localhost:8000/api/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "name": "示例任务",
    "url": "https://example.com",
    "selector": "title"
  }'</code></pre>

  <h2>常见问题</h2>

  <h3>Q1: 安装依赖时编译失败？</h3>
  <p>确保已安装编译工具：</p>
  <pre><code class="language-bash"># Ubuntu/Debian
sudo apt install build-essential python3-dev

# macOS
xcode-select --install</code></pre>

  <h3>Q2: 数据库连接失败？</h3>
  <p>检查 PostgreSQL 服务是否启动：</p>
  <pre><code class="language-bash"># Ubuntu/Debian
sudo systemctl start postgresql

# macOS
brew services start postgresql</code></pre>

  <h3>Q3: 端口被占用？</h3>
  <p>修改 .env 中的 PORT 或查找占用进程：</p>
  <pre><code class="language-bash"># 查找占用 8000 端口的进程
lsof -i :8000
# 或
netstat -ano | findstr :8000</code></pre>

  <h3>Q4: Playwright 无法启动？</h3>
  <p>安装系统依赖：</p>
  <pre><code class="language-bash">playwright install-deps</code></pre>

  <h2>下一步</h2>
  <p>环境搭建完成，现在让我们 <a href="/demo/">看看效果</a>，或者深入了解我们的 <a href="/diary/">开发日志</a>！</p>
</article>
