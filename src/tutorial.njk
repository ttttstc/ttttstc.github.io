---
layout: base.njk
title: 安装教程 - 龙虾历险记
---

<header class="page-header">
  <h1 class="page-title">OpenCRAW 安装入门</h1>
  <p class="page-description">手把手教你搭建开发环境</p>
</header>

<article class="content">
  <h2>环境准备</h2>
  <p>在开始之前，请确保你的开发环境满足以下要求：</p>
  <ul>
    <li><strong>操作系统</strong>：Linux / macOS / Windows (WSL2 推荐)</li>
    <li><strong>Python</strong>：3.10 或更高版本</li>
    <li><strong>Node.js</strong>：18.x 或更高版本（用于前端和工具链）</li>
    <li><strong>Git</strong>：用于代码版本管理</li>
    <li><strong>Docker</strong>（可选）：用于容器化部署</li>
  </ul>

  <h2>安装步骤</h2>

  <h3>1. 克隆项目</h3>
  <pre><code class="language-bash">git clone https://github.com/openclaw/openclaw.git
cd openclaw</code></pre>

  <h3>2. 创建虚拟环境</h3>
  <pre><code class="language-bash"># 使用 venv
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows</code></pre>

  <h3>3. 安装依赖</h3>
  <pre><code class="language-bash">pip install -r requirements.txt</code></pre>

  <h3>4. 配置环境变量</h3>
  <pre><code class="language-bash">cp .env.example .env
# 编辑 .env 文件，配置数据库、Redis 等</code></pre>

  <h3>5. 初始化数据库</h3>
  <pre><code class="language-bash">python scripts/init_db.py</code></pre>

  <h3>6. 启动服务</h3>
  <pre><code class="language-bash"># 开发模式
python -m openclaw dev

# 或使用 Docker
docker-compose up -d</code></pre>

  <h2>验证安装</h2>
  <p>服务启动后，访问以下地址验证：</p>
  <ul>
    <li>管理后台：<a href="http://localhost:8000/admin">http://localhost:8000/admin</a></li>
    <li>API 文档：<a href="http://localhost:8000/docs">http://localhost:8000/docs</a></li>
  </ul>

  <h2>常见问题</h2>

  <h3>Q1: 安装依赖时报错</h3>
  <p>确保 Python 版本 >= 3.10，并使用虚拟环境隔离依赖。</p>

  <h3>Q2: 数据库连接失败</h3>
  <p>检查 .env 中的数据库配置，确保 PostgreSQL 服务已启动。</p>

  <h3>Q3: 端口被占用</h3>
  <p>修改 .env 中的 PORT 配置，或关闭占用端口的其他服务。</p>
</article>
